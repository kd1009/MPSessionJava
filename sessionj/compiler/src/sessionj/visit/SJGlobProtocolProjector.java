package sessionj.visit;

public class SJGlobProtocolProjector
{
	
	// get SJGlobSessionType
	
	// get prefix
		//if Participant x is principal
			//add SJSessionType to new SessionType
		
		//else if Participant x is partner
			//add the dual SJSessionType to new SessionType
	
		//else (if neither principal nor partner: IGNORE)
	
	// do the same for all children
	
}
